# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/

# Environment
.env

# Raw fetch cache (regenerable from APIs; large; stays out of git)
data/raw/

# NOTE: data/nu.duckdb IS committed -- the hosted Streamlit dashboard reads it on
# startup, and it's only ~1.3 MB. To refresh from the APIs:
#   python -m nu_monitor ingest-all
# then commit the updated data/nu.duckdb.

# OS / editor
.DS_Store
.vscode/
.idea/
